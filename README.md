# Arbitrage-bot

This a bot that can arbitrage between WETH and DAI, on multiple UniswapV2 implementations. Get the exchange data from the exchanges, convert it into price data, and use any price mismatches to simulate trades against each other to capture the spread.
